rem #!/bin/sh
rem # you will need to read the top level README, and run boostrap.py
rem # and buildout in order to make pyjsbuild

rem options="$*"
rem #if [ -z $options ] ; then options="-O";fi
..\..\bin\pyjsbuild.bat --print-statements  Hello
